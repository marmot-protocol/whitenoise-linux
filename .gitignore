/build/
/vendor/

# odin build output when built in-package
/app/app

# local notes, not for the repo
/report.md
.stignore
.syncthing.*
*.sync-conflict*

# Screenshots WN_SHOT drops in the working directory
/wn-odin-shot.png

# gettext msgmerge backups — regenerated on every catalog update
*.po~

# stray build artifact, removed from tracking
/app.bin

# flatpak-builder state for a local packaging/flatpak build
/.flatpak-builder/
